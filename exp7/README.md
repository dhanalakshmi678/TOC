AIM

To write a C program to check whether a given string belongs to the grammar
S → 0S1 | ε

ALGORITHM
Start the program.
Read a string from the user.
Count the number of 0s at the beginning of the string.
Count the number of 1s at the end of the string.

Check whether the string contains only the form:

0
n
1
n
Check whether the number of 0s is equal to the number of 1s.
If both conditions are satisfied, accept the string.
Otherwise, reject the string.
Display the result.
Stop the program.

RESULT

Thus, the C program to check whether a given string belongs to the grammar S → 0S1 | ε was successfully implemented and executed. The program correctly identifies strings of the form 0
n
1
n
.
